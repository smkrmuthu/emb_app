import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smkrmuthu.explainmybill',
  appName: 'Explain My Bill',
  webDir: 'dist'
};

export default config;
